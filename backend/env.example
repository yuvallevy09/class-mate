PORT=3001

# CORS (comma-separated). Vite default is http://localhost:5173
CORS_ORIGINS=["http://localhost:5173"]

# Database (will be used once Postgres + SQLAlchemy are wired)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/classmate

# JWT / cookies (placeholder values; change in prod)
JWT_SECRET=dev-change-me
JWT_ACCESS_TTL_SECONDS=900
JWT_REFRESH_TTL_SECONDS=1209600

# Cookie options
COOKIE_SECURE=false
COOKIE_DOMAIN=
COOKIE_SAMESITE=lax


