# Backend

Minimal Express backend scaffold.

## Run (dev)

```bash
cd backend
npm install
npm run dev
```

## Env

Copy `env.example` to `.env` (optional) and adjust values.

## Endpoints

- `GET /health`
- `GET /api/hello`


