VITE_API_URL=http://localhost:3001

VITE_CHAT_ENABLED=true

# Used for UI hints (the backend enforces the real limit via UPLOAD_MAX_SIZE_BYTES).
# Backend is currently configured for 2GB uploads (UPLOAD_MAX_SIZE_BYTES=2147483648).
VITE_UPLOAD_MAX_SIZE_MB=2048



